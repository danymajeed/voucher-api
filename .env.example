# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=voucher_db
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@$localhost:5430/${POSTGRES_DB}?schema=public

# JWT
JWT_SECRET="your-super-secret-key-change-in-production"
JWT_EXPIRATION="1h"
JWT_REFRESH_EXPIRATION="7d"

# App
PORT=3433
NODE_ENV=development

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100